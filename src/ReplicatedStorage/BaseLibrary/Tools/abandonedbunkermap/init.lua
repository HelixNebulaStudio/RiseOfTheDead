local Debugger = require(game.ReplicatedStorage.Library.Debugger).new(script);
--==
local generic = require(game.ReplicatedStorage.Library.Tools.ToolGenerics:WaitForChild("genericMap"));

return generic.Inherit("Raid", "Abandoned Bunker", "Raid Abandoned Bunker");
