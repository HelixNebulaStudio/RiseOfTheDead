local Debugger = require(game.ReplicatedStorage.Library.Debugger).new(script);
--==
local generic = require(game.ReplicatedStorage.Library.Tools.ToolGenerics:WaitForChild("genericCrate"));

return generic.Inherit(script.Name, {
	Core={Id=85979089955779;};
	Placing={Id=4527422890};
});