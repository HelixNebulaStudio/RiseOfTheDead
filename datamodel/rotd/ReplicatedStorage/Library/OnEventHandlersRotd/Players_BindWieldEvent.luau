local Debugger = require(game.ReplicatedStorage.Library.Debugger).new(script);

local RunService = game:GetService("RunService");
--==
return function(event: EventPacket, action: string, toolHandler: ToolHandlerInstance)
end