local Debugger = require(game.ReplicatedStorage.Library.Debugger).new(script);
--==
local RunService = game:GetService("RunService");

local Dialogues = {};
--==

-- MARK: Skipper
-- Dialogues.Skipper = {};
-- Dialogues.Skipper.DialogueHandler = function(player, dialog, data, mission)

-- end

return Dialogues;
